# Brand assets — drop your logo files here

Same convention as the POS app — see `apps/pos/public/README-logos.md` for full instructions.

Required files in this directory:
- `logo.png`        — colour logo, used in login + sidebar
- `logo-mono.png`   — optional monochrome variant
- `favicon.ico`     — browser tab icon
